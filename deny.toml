[advisories]
ignore = [
    # rsa timing sidechannel via russh - no fix available upstream.
    "RUSTSEC-2023-0071",
    # `instant` unmaintained, pulled transitively by notify -> notify-types ->
    # innerwarden-dna. No safe upgrade available upstream; the notify crate
    # still depends on instant.
    "RUSTSEC-2024-0384",
<<<<<<< HEAD
    # `paste` unmaintained. Only enters the tree when the `local-classifier`
    # feature is on (via tokenizers + tract-core). Macro crate with zero
    # runtime surface; no safe alternative today.
    "RUSTSEC-2024-0436",
    # Removed (confirmed absent from the dep graph with `--all-features`):
    # - RUSTSEC-2025-0134 (rustls-pemfile): dropped when axum-server bumped
    #   0.7 -> 0.8 in PR #230.
=======
    # rustls-pemfile unmaintained - transitive dep via axum-server, no upgrade path yet
    "RUSTSEC-2025-0134",
    # paste unmaintained - transitive via tokenizers + tract-core behind optional
    # local-classifier feature only. Macro crate with zero runtime surface.
    "RUSTSEC-2024-0436",
>>>>>>> origin/main
]

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "OpenSSL",
    "Zlib",
    "BSL-1.0",
    "CC0-1.0",
    "MPL-2.0",
    "CDLA-Permissive-2.0",
    "BUSL-1.1",
]

[bans]
multiple-versions = "allow"
wildcards = "allow"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = [
    "https://github.com/InnerWarden/innerwarden-mesh",
]
